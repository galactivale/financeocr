const express = require('express');
const router = express.Router();

// Placeholder for compliance routes
router.get('/', (req, res) => {
  res.json({
    success: true,
    message: 'Compliance endpoint - Coming soon',
    data: [],
  });
});

module.exports = router;
